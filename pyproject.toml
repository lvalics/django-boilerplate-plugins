[project]
name = "django-boilerplate-plugins"
version = "0.1.0"
description = "Installer + plugins for the free django-boilerplate (and PRO Pegasus)."
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
